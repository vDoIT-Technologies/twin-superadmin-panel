# SuperAdmin backend base URL. When unset, the panel runs on synthesized demo data.
# Example (local backend): http://localhost:5000
VITE_API_BASE_URL=

# Optional: SuperAdmin bearer token for local dev. In normal use the token is read
# from localStorage ('accessToken'); this is only a convenience fallback.
VITE_API_TOKEN=
